<Window x:Class="Example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example"
        xmlns:color="clr-namespace:WpfColorPicker;assembly=WpfColorPicker"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        x:Name="mainWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <color:SaturationBrightnessPicker Grid.Column="0" SelectedColor="{Binding C, ElementName=mainWindow}"/>
        <color:HuePicker x:Name="hp" Grid.Column="1" SelectedColor="{Binding C, ElementName=mainWindow}"/>
        <Label Grid.Row="1" Content="{Binding C, Mode=TwoWay, ElementName=mainWindow}"></Label>
    </Grid>
</Window>
